//7-2 2018Final简单字符串string处理（三人行）
#include<iostream>
#include<string>
using namespace std;
int main()
{
	string t,tt,a,b,c;
	getline(cin,t);
	getline(cin,a);
	getline(cin,b);
	getline(cin,c);
	for(int i=0;i<t.length()-1;i++)
	{
		for(int j=i;j<t.length()-1;j++)
		{
			
		}
	}
	for(int i=0;i<a.length()-1;i++)
	{
		if(a[i]>='A'&&a[i]<='a')a[i]+=32;
		
	}
}

